<h1>To Do List</h1>

<ul>
  <% @projects.each do |project| %>
  <li>
  	<%= link_to project.title, project_path(project.id) %>
  	<%= link_to "編集", edit_project_path(project.id) %>
  	<%= link_to "削除", project_path(project.id), method: :delete, data: { confirm: "よろしいですか?" } %>
  </li>
  

  <% end %>



</ul>

<p><%= link_to "新規作成", new_project_path %></p>
